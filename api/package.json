{
  "name": "stripe-eshop-no-admin-panel-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:dev": "nodemon --require dotenv/config ./server.js"
  },
  "devDependencies": {
    "dotenv": "^16.0.3",
    "nodemon": "^2.0.20"
  },
  "dependencies": {
    "express": "^4.18.2",
    "stripe": "^11.8.0"
  }
}
